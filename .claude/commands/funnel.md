Cambia el funnel activo para esta conversación.

El funnel seleccionado es: **$ARGUMENTS**

Según el funnel indicado, anuncia cuál es el archivo de contenido que editaremos y confirma el cambio. Usa este mapeo:

- `seed-mexico` → `src/app/content/funnel-copy.ts`
- `seed-mexico-copia` → `src/funnels/seed-mexico-copia/copy.ts`

Si el argumento no corresponde a ningún funnel registrado en `src/config/funnels.ts`, indícalo y lista los disponibles.

Guarda en memoria el funnel activo para usarlo en el resto de la conversación. Responde de forma breve confirmando el cambio.
